Q) CoffeeScript compiles to:
  - MSIL
  * JavaScript
  - Java bytecode
  - Machine code
  = liam-mclennan|what-is-coffeescript|2

Q) CoffeeScript code blocks are denoted by:
  - Curly braces 
  - Parenthesis
  - Semi-colon
  * Indentation
  = liam-mclennan|language-elements|3

Q) The CoffeeScript syntax for functions is:
  * ->
  - function() {}
  - =>
  - {|| }
  = liam-mclennan|data-types|7

Q) The CoffeeScript syntax for string interpolation is:
  - "Hello " + name 
  - "Hello @name"
  - "Hello %{name}"
  * "Hello #{name}"
  = liam-mclennan|data-types|2

Q) CoffeeScript line comments begin with:
  * #
  - //
  - -- 
  - '
  = liam-mclennan|language-elements|2

Q) The correct syntax for assigning a variable in CoffeeScript is:
  * count = 0
  - dim count = 0
  - var count = 0
  - count = (-> 0)()
  = liam-mclennan|language-elements|2

7. Given the expression [x, [y1, y2]] = [-27, [1, 2]] what is the value of y1?
  - null
  - -27
  - undefined
  * 1
  = liam-mclennan|language-elements|2

8. What is the keyword for the negative conditional operator (ie the inverse of if)?
  * unless
  - until
  - not
  - if !
  = liam-mclennan|language-elements|3

9. Which of these is a CoffeeScript ternary if expression?:
  - condition ? "yes" : "no"
  - switch condition 
       when true then "yes"
       when false then "no"
  - if condition then "yes" else "no"
  - iif(condition, "yes", "no")

10. Which of the following is/are valid loop operators in CoffeeScript:

  - until
  - while
  - for 
  - All of the above

11. The 'when' keyword:

  - Defines a branch of a switch statement
  - Applies an object context to a code block
  - Applies a guard to an array comprehension
  - A and C

12. The textual logical operator equivalent to !== is:

  - isnt
  - is not
  - not is
  - !=

13. The expression 1 < 5 < 3 + 2 evaluates to:

  - true
  - undefined
  - NaN
  - false

14. What is the value of this expression? '#{1+1}'

  - 2
  - '#{1+1}'
  - two
  - '2'

15. The character sequence that denotes the start of a multiline string with interpolation is:

  - <<
  - ###
  - """
  - '''

16. Which of the following will produce an array containing all of the even numbers between 1 and 10 (inclusive)?:

  - (i for i in [1..10] when i % 2 is 0)
  - (i for i in [2..10] by 2)
  - (2*i for i in [1..5])
  - All of the above

17. Which of these expressions will return 'ray'?

  - 'Array Slicing'[1..4]
  - 'Array Slicing'[3..5]
  - 'Array Slicing'[2...5]
  - 'Array Slicing'[2,4]

18. Extended regular expressions are denoted with:

  - /
  - ###
  - '''
  - ///

19. What is the value of the following expression? (-> (-> 1 ))()

  - A function
  - null
  - Compile error
  - 1

20. What is the value of the following expression? (-> (-> 1 ))()()

  - A function
  - null
  - Compile error
  - 1

21. What is the value of the following expression? ((x = 'a')-> x[0])('1')

  - 'a'
  - '1'
  - 1
  - '0'

22. What is the purpose of the 'fat arrow' ( => )?

  - To separate keys and values in a hash
  - To indicate directionality
  - To bind the value of 'this' when defining a function 
  - All of the above

23. What is the keyword that defines a CoffeeScript class?

  - def
  - new
  - klass
  - class

24. When defining a class how do specify that a property should have private visability?:

  - private keyword
  - you can't
  - prefix with _
  - none of the above

25. Which of the following defines a class B that inherits from A?

  - class B : A
  - class A extends B
  - class B extends A
  - class A : B